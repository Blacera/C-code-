#include <stdio.h>
#include <math.h>
int main()
{
    int n;
    scanf ("%d",&n) ;
    int lim = (n - 1) / 2; // giá trị của lim chính là giá trị ta cần tìm 
    printf("%d",lim);
    return 0;
}